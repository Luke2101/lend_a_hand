import type {NextFunction} from "connect";
import type {Request, Response} from "express";
export class AuthHandler {
    public static checkRequest(req: Request, res: Response, next: NextFunction): void {

    }
}